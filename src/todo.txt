
 * Introduce heuristic for pivot selection in decision tree
 * Add Array pattern
 * Tidy up in Dispatch.jl, move some into DecisionTree?
 * Make sure create_dispatch creates methods in topological order
 * Improve show_dispatch output
   - Better symbol names
 * Remove dependency on Toivo -- introduce Meta module instead?
